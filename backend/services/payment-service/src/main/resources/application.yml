server:
  port: 8084

spring:
  application:
    name: payment-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/elearning_payments}
    username: ${SPRING_DATASOURCE_USERNAME:elearning}
    password: ${SPRING_DATASOURCE_PASSWORD:elearning}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:elearning}
    password: ${RABBITMQ_PASSWORD:elearning}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
